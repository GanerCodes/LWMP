TOKEN ─ ᔐ⁶􍦛⁶⁴
UUID ─ ᔐ⁸􍦛⁶⁴  [a-zA-Z0-9]
NAME ─ ᔐ¹􍦛⁶⁴
RGB ─ [r:U8, g:U8, b:U8]
ATOM_PARAM ╥ Static ─ RGB
           ╟ Rainbow ─ [segs:ℝ, s:U8, v:U8]
           ╙ Fade ┬ clrs  ─ *RGB
                  ├ speed ─  ℝ⁰􍦛􋔛  transitions/s
                  └ sharp ─  ℝ⁰􍦛¹
MODE ┬ effects ─ *EFFECT ╥ Rotate ─ [speed:ℝ, offset:ℝ]  led/s and leds
     │                   ╟ Reverse ─ □
     │                   ╙ Brightness ─ ℝ⁰􍦛¹
     └╥ modes ─ *MODE
      ╙ atom ─ [ℕ⁺:ledc, ATOM_PARAM]
SCENE ┬ offs ─ UUID ↦ ℕ⁺
      └ mode ─ MODE
SCENE_ACTIVATION ─ [NAME, 𝔹:que, ℝ⁰􍦛􋔛:dur]

§ FRONTEND STORAGE
USER ─ TOKEN ↦ LAYOUT ┬ scenes ─ NAME ↦ SCENE
                      └ devs   ─ UUID ↦ DEV ┬ name      ─ NAME
                                            ├ ledc      ─ ℕ⁺
                                            ├ rgb_order ─ U32
                                            ├ reverse   ─ 𝔹
                                            └ ws_url    ─ ᔐ¹􍦛²⁵⁶

§ API
COMMAND ┬ token ─ TOKEN
        └╥ Get_devs ↪ UUID ↦ DEV
         ╟ Change_dev ┬ uuid ─ UUID
         ║            └ dev ─ DEV  all fields optional, also "delete:true" to del; also allows for token,R_SSID,R_PASS
         ╟ Set_scene ┬ uuids ─ *UUID  ← remove this field once we have proper DB storage of user scenes?
         ║           └ scene ─ SCENE_ACTIVATION
         ║           ↪ *𝔹
         ╟ Del_scene ┬ uuids ─ *UUID
         ║           └ scene ─ NAME
         ║           ↪ *𝔹  if the scene existed prior
         ╟ Push_scenes ┬ uuids ─ *UUID
         ║             └ scenes ─ NAME ↦ SCENE
         ║             ↪ *ℕ  number of sucessfully saved scenes
         ╟ Pull_scenes ─ uuids ─ *UUID
         ║             ↪ *(NAME ↦ SCENE)
         ╙ Set_schedule ┬ uuids ─ *UUID  ← remove this field once we have proper DB storage of user scenes?
                        └ schedule ─ ℕ ↦ SCENE_ACTIVATION  key is seconds from UTC week start
 󷹇 WS msg to LW is very similar, same name and (at least currently) just sending the one relevent value 

§ DATABASE
tok𝘋𝘉: token ↦ devs ─ UUID*
dev𝘋𝘉: uuid ↦ {token name ledc order reverse}

§ MISC
󷹇 From server to device the `Set_scene` also includes a UTC timestamp in ms to sync to.

𝔠: encode a multi-choice value
𝔪: decode said multi-choice value