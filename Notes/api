TOKEN ─ ᔐ⁶􍦛⁶⁴
UUID ─ ᔐ⁸􍦛⁶⁴  [a-zA-Z0-9]
NAME ─ ᔐ¹􍦛⁶⁴
RGB ─ [U8,U8,U8]
MODE ┬ effects ─ *EFFECT ╥ Reverse ─ □
     │                   ╟ Brightness ─ ℝ⁺
     │                   ╙ Rotate ┬ speed  ─ ℝ  led/s
     │                            └ offset ─ ℝ
     └╥ Modes ─ *MODE
      ╙ Atom ┬ leds ─ ℕ⁺
             └╥ Static ─ clr ─ RGB
              ╟ Rainbow ┬ segs ─ ℝ
              ║         ├ sat  ─ U8
              ║         └ val  ─ U8
              ╙ Fade ┬ clrs  ─ *RGB
                     ├ speed ─  ℝ⁰􍦛􋔛  transitions/s
                     └ sharp ─  ℝ⁰􍦛¹
USER ─ TOKEN ↦ LAYOUT ┬ scenes ─ NAME ↦ SCENE ┬ devs ─ *UUID
                      │                       └ mode ─  MODE
                      └ devs   ─ UUID ↦ DEV ┬ name      ─ NAME
                                            ├ ledc      ─ ℕ⁺
                                            ├ rgb_order ─ U32
                                            ├ reverse   ─ 𝔹
                                            └ ws_url    ─ ᔐ¹􍦛²⁵⁶

COMMAND ┬ token ─ TOKEN
        └╥ Get_devs ─ □
         ║          ↪ UUID ↦ DEV
         ╟ Set_scene ─ SCENE_ACTIVATION ┬ scene  ─ NAME
         ║                              └ timing ─ TIMING ┬ que ─ 𝔹
         ║                                                └ dur ─ ℝ⁰􍦛􋔛
         ╟ Change_dev ┬ uuid ─ UUID
         ║            └ dev ─ DEV  all fields optional, also "delete:true" to del, also allows for token,R_SSID,R_PASS
         ╟ Push_scenes ┬ uuid ─ UUID
         ║             └ scenes ─ NAME ↦ SCENE
         ╟ Set_schedule ─ ℕ ↦ SCENE_ACTIVATION  key is seconds from UTC week start
         ╙ Pull_scenes ─ uuid ─ UUID
                       ↪ NAME ↦ SCENE

tok𝘋𝘉: token ↦ devs ─ UUID*
dev𝘋𝘉: uuid ↦ {token name ledc order reverse}

󷹇 From server to device the `Set_scene` also includes a UTC timestamp in seconds to sync to.

𝔠: encode a multi-choice value
𝔪: decode said multi-choice value