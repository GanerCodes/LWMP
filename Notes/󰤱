DONE
  • Added the logo
  • Activate panel
  • Scenes listing duplicate option
  • Clicking on buttons doesn't select text anymore
  • Phone gestures don't happen when dragging sliders anymore.
  • Redid how the preview scaling works (for the like 3rd time lol).
  • Device listing menu expansion
  • Device renaming, reversing, token-changing
  • Added indication which token is actually active
  • Fixed issue of delting active token
  • Made sure we check the most updated device listings inside scenes panel
  • Stopped resizers appearing in the chooser screens
  • Added text validation things
  • Dragging one element into another should make a tooltip menu with `swap` and `replace`
  • Added smoothing functions to sliders so smaller numbers are easier to set.
  • Made grouping option work better, automatically selects all intermediate elements. You can remove from the top or bottom or click within it to reset the selection.
  • Submit/cancel buttons in element picker mode are in a consistant place now
  • Added changing between AP/WiFi modes
  • Added LOG_LEVEL setting to reduce stdout for performance
  • Added deleting dev from DB function
TODO
  • Deletion option
    • "Delete_dev" Endpoint
    • UI implementation
    • Note: only applicable for an offline device
  • Security
    • I don't trust humanity enough to gracefully handle that leaking a token lets an attacker add all your devs to a botnet, so I need UPDATE_URL/WS_URL to be changable in, and only in, AP mode
  • LW-Controller
    • Performance
      • Redo how scene data is calulated because it can prob crash the device for too complex scenes
      • RAM usage is kinda cooked and SSL is often to blame
    • Bugs
      • Random disconnects/freezes. unfortunately I suspect it's that stupid websocket SSL bug which is by-and-large the worst problem i've had to deal with this entire project
  • Advanced device options
    • Setting recalb time (atm it's 3am user time, and is set when doing RGB or LEDC calib)
    • Pulling scenes
    • Websocket/update URLs
    • Change bit timing (useful for controlling certain nonstandard ws*-like LED strips)
    • Change pin?
  • UI
    • Scene scheduler
    • Re-style onboard AP page
    • Improvements
      • Add instructions/directions/re-style for the add token prompt
      • Enter hex code for colors (and in general click into and type out slider)
      • Making the delete and ungroup options be hidden and just appear somewhere as an obvious option when you start dragging an element and you can just stick it into the option
      • For element picker modes scroll such that the the closest valid element is centered if it's not at least ≈10% visible already
    • Bugs
      • Sliders/value labels inside of fade mode is jank
      • The lower menu in device listing doesn't close automatically and reopens when re-entering the devices tab
    • Misc
      • 󰖪 and 󰖩 to indicate AP and WiFi seems a bit weird, both are wireless
  • Technical stuff:
    • Proxy the token api with scene stuff
    • itemGridEditable needs the ability to directly control and modify elements
  • Dumb stuff
    • I'm like 87.882523% sure that I commited wifi creds again 💀

OLD PROGRESS TRACKING:
  Done
    𝟏
      Reworked WiFi/AP things
        Actual DNS trap, it now shows the popup when you connect to the AP
          (at least on an iPhone 6 lol)
        Got LED display working while HTTP and DNS servers run
        Auto-restarts when in AP every 30 minutes when no connections/events
          e.g. so now if your internet goes you don't need to unplug/replug lightwaves to get them to get out of setup mode
      Fixed the dumb issues with flashing devices
        (it also flashes faster and doesn't need fiddling with the button)
      Device code cleanup
      NTP Time (hopefully the like ±30ms is precise enough!!1)
        (LOL YOU THOUGHT YOU HAD NTP WORKING AT THIS POINT HAHAHA BROTHER ITS LIKE 4 WEEKS LATER AND IT ONLY NOW IS ACCURATE)
    𝟐
      Database locks correctly now
        We can do database things and talk w/ LW in the same request
          (Important part is without freezing the database for the duration of the request)
      Actual API flowthrough finished
        LW scene storage, loading, deleting
          Comes pre-equiped with some scenes
          ✓ (tested ✓) API Pull_scenes (dumps all stored scenes)
          ✓ (󰤱 test) API Push_scenes (bulk uploads scenes)
          ✓ (󰤱 test) API Del_scene   (deletes a scene)  
          󰤱 (dummy implementation used) Set_schedule
          󰤱 (dummy implementation used) Set_scene (contingent on more work on the LED-mode side of things)
        Change_dev API implemented :>
      Change_dev API: Changing device parameters (working ✓, mostly tested)
        Dynamic LED parameter changing (takes < ≈1 second to update any of the following)
          LED count, RGB order, output PIN, timing
        Change stuff about the device remotely now
          Name, token, router credentials, websocket URL
            Automatically restarts relevent services
              router creds ⇒ resets internet entirely
              websocket URL ⇒ resets only websocket
    𝟑
      Finish bidirectional websocket protocol implementation
      Add to API file the returns of various methods from LW
    𝟒
      I stopped keeping track lol

  fix bad scenes cause dev to bootloop lol,
  ⟨['*', ['Push_scenes', {'raw': {'mode': {'fx': [[1, 0, 0]], '*': [{'*': [{'1': [50, 0, 16711680]}, {'1': [50, 0, 65280]}, {'1': [50, 0, 255]}]}, {'fx': [[1, 25, 0]], '*': [{'1': [5, 1, 3, 245, 50]}, {'1': [45, 0, 4456448]}]}, {'fx': [[1, 25, 0]], '*': [{'1': [22, 1, 1, 245, 50]}, {'1': [15, 0, 17408]}]}, {'fx': [[1, 25, 0]], '*': [{'1': [33, 1, 1, 245, 50]}, {'1': [25, 0, 68]}]}, {'fx': [[1, 25, 0]], '*': [{'1': [2, 1, 1, 245, 50]}, {'1': [120, 0, 4456516]}]}]}, 'offsets': {'testdevice1': 'testdevice150', 'testdevice0': 'testdevice050', 'testdevice2': 'testdevice250', 'testdevice3': 'testdevice3'}}}], ['Set_scene', 'raw', False, -1, 1776004828852]]⟩ → testdevice3



  Planned
    General
      Finish API things
    Bugs
      https://ganer.xyz/s/43f776d9d071ec69
    Features
      ✓ Remote update ability
      Prob sync scene names with database?
      ✓ Offline AP mode (screen with buttons for the various stored modes)
      Simple Calibration Function
        RGB : Screen #1: Choose the current strip color (R G B)
              Screen #2: Choose the current strip color (R G B) ∖ {Choosen in screen 1} (include reset button)
        LEDC: [works by having #0F0 followed by #F00]
              [start at like idk 100]
              The strip will transition from green to red at LED 100. Can you see any red?
                Y → binary search right [so 200 for step 1]
                N → binary search left  [so 50 for step 1]
                Reset button
              (Note: first streak of Y presses just doubles until 2000 LEDs)
              [ᵉᵍ for 300 LEDS we have ⇒ YNNYYYYYYY
                                         400 300 250 275 287 293 296 298 299 ⇒ 300]
      Settings Menu
      10 Preset Scenes
      Mobile App Experience
      UI Design Update
      ✓ Timer Function (w/ calendar)
      <Conditional>
          Create Bakkes Mod Plugin (cool, low priority)
          Generate LED Preview (cool, low priority)
        For like, comically large amount of time and money
          HDMI Passthrough Capabilities
        Maybe?
      <N/A>
        Fix LED Count Update Bug