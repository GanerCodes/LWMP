/* AUTOGENERATED */
 let ətileButton = (t, cb, þF0284 = t) => mkə(`button`, [
   [`𝐶`, `bigButton`],
   [`𝑆`, fit_size(þF0284, 250)],
   [``, (...𝔸) => cb(t)]
 ], þF0284)

 const əpromptText = (cb, instr, fld) => {
   const fldþ0E27F = textField(fld);
   const þ0E27F = popup({
       þ0F2D4: true,
       þF0159: true
     },
     ətitledBox(
       instr,
       mkə(`div`, [
         [`𝐶`, `promptBox`],
         [`𝑆`, `max-width: ⅘; display: flex; flex-direction: column;`]
       ], [
         fldþ0E27F,
         textButton(`Submit`, (...𝔸) => {
           þ0E27F.close();
           cb(fldþ0E27F.𝚒.value)
         })
       ]), {
         outline: false
       }));
 }

 let ətokenListing = (cb) => {
   const prompt_token = (x, ...𝔸) => əpromptText(x, `Enter token to add`, {
     𝑃𝐻: `Token`,
     þ0F070: true
   });
   const þ0E27F = əitemGridEditable(
     (gen) => prompt_token((x, ...𝔸) => (add_check_tok(x), gen(x))),
     (x, rem) => [ətileButton(x, cb, x.replace(/(?<=.).(?=.)/g, "⋅")), əbuttonBar(["󰆴", rem])],
     del_tok);
   þ0E27F.populate = (𝐓) => {
     þ0E27F.clear_items();
     𝐓.forEach((x, ...𝔸) => þ0E27F.add_item(x));
   }
   return þ0E27F;
 }

 let ədevListing = (cb = þF0EFE) => {
   const 𝔖 = {};
   const ədevTile = (𝐮, 𝐝) => {
     const clr = `#${𝐝 . online ? `8CD521` : `D54C21`}`;

     const name1 = 𝐝.NAME || 𝐮;
     const name2 = name1 != 𝐮 ? 𝐮 : undefined;
     const þ0E27F = mkə(`div`, [
       [`𝑆`, `position: relative; min-height: 125px; display: flex; flex-direction: column; align-items: flex-start; padding-left: 10px; padding-right: 10px; padding-top: 6px; padding-bottom: 6px; font-size: 20px; font-weight: 100;`]
     ], [(mkə(`T`, [
       [`𝑆`, `font-weight: 400; position: absolute; top: -7px; right: 10px; font-size: 50px; color: ${clr};`]
     ], `⬤`)), (mkə(`T`, [
       [`𝑆`, `font-weight: 400; font-size: 30px;`]
     ], `${name1}`)), (name2 ? mkə(`T`, false, `UUID: ${name2}`) : []), (mkə(`T`, false, `${𝐝 . RGB_ORDER} - ${𝐝 . LEDC} pixels`)), (mkə(`T`, false, `Direction: ${[`For` , `Back`][ + 𝐝 . REVERSE]}wards`))]);
     return þ0E27F
   };
   const þ0E27F = əitemGridEditable(
     undefined,
     ([𝐮, 𝐝], rem) => {
       const set = (𝚜) => Bþ0E27F.ᴍþF01A9((x, ...𝔸) => x.ᴍ(þF0EFE)).þF01A9().ᴍ((x, ...𝔸) => x.disabled = !𝚜);
       const 𝐚 = 𝐀(𝔖.𝐭, 𝐮);

       const þ0F044ƒ = () => əpromptText(async (x, ...𝔸) => {
           set(false);
           await 𝐚.config({
             NAME: x
           });
           þ0E27F.populate();
           set(true);
         },
         `Enter new device name`, {
           𝑃𝐻: `Name`
         });
       const þ0F021ƒ = async () => {
         set(false);
         await 𝐚.config({
           REVERSE: !𝐝.REVERSE
         });
         þ0E27F.populate();
         set(true);
       };
       const Bþ0E27F = [
         əbuttonBar(["", (...𝔸) => prompt_rgb_calibrate(𝐚, (x, ...𝔸) => (x, þ0E27F.populate()))],
           ["", (...𝔸) => prompt_led_calibrate(𝐚, (x, ...𝔸) => (x, þ0E27F.populate()))],
           ["󰐥", (...𝔸) => 𝐚.off()],
           ["", (...𝔸) => Bþ0E27F[1].þF09E5(`dropped`, Bþ0E27F[1].þF1021(`dropped`) == "✓" ? "✗" : "✓")]),
         əbuttonBar(["", þ0F044ƒ],
           ["", þ0F021ƒ])
       ]
       Bþ0E27F[1].þF09E5(`dropped`);
       return [ədevTile(𝐮, 𝐝), ...Bþ0E27F];
     },
     (x, ...𝔸) => print(`Removing ${x}`),
     [], {
       show_plus: false,
       placeholder: (...𝔸) => mkə(`h2`, false, `No devices found for this token...`)
     })
   þ0E27F.populate = async (𝐭) => {
     if (𝐭) 𝔖.𝐭 = 𝐭;
     let devs = await load_devices(𝔖.𝐭);
     devs = devs[1];
     þ0E27F.clear_items();
     𝒪(devs).forEach((x, ...𝔸) => þ0E27F.add_item(x));
     cb(devs);
     return devs;
   };
   return þ0E27F;
 };

 let əsceneListing = (cb) => {
   const 𝔖 = {};
   const prompt_scene = (cb, Ѧ = {
     name: "",
     𝐔: [],
     dat: {}
   }) => {
     const ədevItem = (𝐮, 𝐝) => {

       const name1 = 𝐝.NAME || 𝐮;
       const name2 = name1 != 𝐮 ? 𝐮 : undefined;
       const þF0284 = `${name1}${name2 ? ` (${name2})` :  "" }`;
       const þ0E27F = mkə(`span`, [
         [`𝐶`, `devDraggable`]
       ], [`󰍜`, mkə(`div`, false, ``), mkə(`T`, [
         [`𝑆`, `text-align: right;`]
       ], þF0284)]);
       þ0E27F.𝐮 = 𝐮;
       return dragItem(þ0E27F);
     };
     const þ0E27Fname = textField({
       𝑃𝐻: `Scene Name`,
       value: Ѧ.name
     });
     let þ0E27FdevsL, þ0E27FdevsR;
     const þ0E27Fdevcols =
       mkə(`span`, [
         [`𝑆`, `display: flex; flex-direction: row; width: 90%; height: 325px; margin-bottom: 10px;`]
       ], [
         mkə(`div`, [
           [`𝑆`, `display: flex; flex-direction: column; width: 50%; font-size: 26px;`]
         ], [`Unused`, dragHold((þ0E27FdevsL = mkə(`div`, [
           [`𝐶`, `devDraggableHolder`]
         ], ``)))]),
         mkə(`div`, [
           [`𝑆`, `display: flex; flex-direction: column; width: 50%; font-size: 26px;`]
         ], [`Used`, dragHold((þ0E27FdevsR = mkə(`div`, [
           [`𝐶`, `devDraggableHolder`]
         ], ``)))])
       ]);
     𝒪k(𝔖.𝐃).ᴍ((x, ...𝔸) => Ѧ.𝐔.includes(x) || þ0E27FdevsL.append(ədevItem(x, 𝔖.𝐃[x])));
     Ѧ.𝐔.ᴍ((x, ...𝔸) => þ0E27FdevsR.append(ədevItem(x, 𝔖.𝐃[x])));
     const get = () => {
       const 𝐔 = þ0E27FdevsR.ᴍ((x, ...𝔸) => x.𝐮);
       const L = 𝐔.ᴍ((x, ...𝔸) => 𝔖.𝐃[x].LEDC);
       const þF0832 = 𝒪𝑏(Ѧ.dat) || þF0832Util.defM(L);
       return {
         ...Ѧ,
         𝐔,
         dat: þF0832,
         name: þ0E27Fname.𝚒.value
       }
     };
     const þ0E27F = popup({
         "": true,
         "󰅙": true
       },
       ətitledBox(
         `Scene details`,
         mkə(`div`, [
           [`𝐶`, `promptBox`],
           [`𝑆`, `max-width: ⅘; display: flex; flex-direction: column;`]
         ], [
           þ0E27Fname,
           þ0E27Fdevcols,
           textButton(`Submit`, (...𝔸) => {
             þ0E27F.close();
             cb(get())
           })
         ]), {
           outline: false
         }));
   };
   const þ0E27F = əitemGridEditable(
     (gen) => prompt_scene(
       (𝐬) => {
         if (!add_scene(𝔖.𝐭, 𝐬))
           return print(`Scene already exists ${x}‽`);
         gen(𝐬);
       }),
     (𝐬, rem) => {
       print(`Generating scene:`, 𝐬);
       const update = (𝚗) => {
         rem(𝐬);
         add_scene(𝔖.𝐭, 𝚗, true);
         þ0E27F.add_item(𝚗);
       };
       const clone = (...𝔸) => þ0E27F.add_item({
         ...structuredClone(𝐬),
         name: `${𝐬 . name} - Copy`
       });
       const buttonþ0E27F = ətileButton(𝐬.name, () => cb({
         𝐬,
         𝔖,
         þF0832: 𝐬.dat
       }));
       if (!𝐬.𝐔.length) buttonþ0E27F.disabled = true;
       return [buttonþ0E27F,
         əbuttonBar(["󰆴", rem],
           ["", (...𝔸) => prompt_editor(update, 𝔖, 𝐬)],
           ["󰆏", clone],
           ["", (...𝔸) => prompt_scene(update, 𝐬)])
       ];
     },
     (𝐬) => move_scene(𝔖.𝐭, 𝐬.name));
   þ0E27F.populate = (𝐭) => {
     if (𝐭) 𝔖.𝐭 = 𝐭;
     𝔖.𝐃 = get_devs(𝔖.𝐭);
     þ0E27F.clear_items();
     𝒪(get_scenes(𝔖.𝐭)).ꟿ((name, y) => þ0E27F.add_item({
       name,
       ...y
     }));
   };
   return þ0E27F;
 }