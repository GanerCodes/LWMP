/* AUTOGENERATED */
 const ətileButton = (t, ƒ, þF0284 = t) => mkə(`button`, [
   [`𝐶`, `bigButton`],
   [`𝑆`, fit_size(þF0284, 250)],
   [``, () => ƒ(t)]
 ], þF0284)
 const əpromptGeneral = (cb, instr, ...𝙴) => {
   const þ0E27F = popup({
       "": true,
       "󰅙": true
     },
     ətitledBox(
       instr,
       mkə(`div`, [
         [`𝐶`, `promptBox`],
         [`𝑆`, `max-width: ⅘; display: flex; flex-direction: column;`]
       ], [
         ...𝙴,
         mkə(false, [
           [`𝑆`, `height: 1em;`]
         ], false),
         textButton(`Submit`, (...𝔸) => {
           þ0E27F.close();
           cb(...𝙴)
         })
       ]), {
         outline: false
       }));
 }

 const ətokenListing = (Ѧ = {}) => {
   Ѧ = {
     ƒ: þF0EFE,
     delƒ: þF0EFE,
     init: true,
     Ⴝ: undefined,
     ...Ѧ
   };
   Ѧ.𝐓 ??= [];

   const prompt_token = (x, ...𝔸) => əpromptGeneral((þ0E27F) => x(þ0E27F.𝚒.value),
     `Enter token to add`,
     textField({
       𝑃𝐻: `Token`,
       þ0F070: true
     }));
   const censor = (x, ...𝔸) => x.replace(/(?<=.).(?=.)/g, "⋅");
   const þ0E27F = əitemGridEditable(
     (gen) => prompt_token((x, ...𝔸) => (add_check_tok(x), gen(x))),
     (𝐭, rem) => {
       if (!Ѧ.𝐓.includes(𝐭)) Ѧ.𝐓.push(𝐭);
       return [ətileButton(censor(𝐭), () => Ѧ.set(𝐭)),
         əbuttonBar(["󰆴", rem])
       ]
     },
     (𝐭) => (Ѧ.delƒ(𝐭), del_tok(𝐭), Ѧ.populate(Ѧ.𝐓.þF0232((x, ...𝔸) => x != 𝐭))));
   Ѧ = þ0E27F.Ѧ = {
     ...þ0E27F.Ѧ,
     ...Ѧ,
     þ0E27F
   };
   Ѧ.populate = (𝐓 = undefined) => {
     if (𝐓 !== undefined) Ѧ.𝐓 = 𝐓;
     if (!Ѧ.𝐓.includes(Ѧ.Ⴝ)) Ѧ.Ⴝ = undefined
     Ѧ.clear();
     𝐓.forEach((x, ...𝔸) => Ѧ.add_item(x));
     Ѧ.set();
   }
   Ѧ.set = (Ⴝ = undefined, call = true) => {
     const oþ0E27F = þ0E27F.children?.[𝚒o = Ѧ.𝐓.indexOf(Ѧ.Ⴝ)];
     const nþ0E27F = þ0E27F.children?.[𝚒n = Ѧ.𝐓.indexOf(Ⴝ)];
     Ѧ.Ⴝ = nþ0E27F ? Ⴝ : undefined;
     if (Ѧ.Ⴝ !== undefined && call) Ѧ.ƒ(Ѧ.Ⴝ);
     oþ0E27F?.þF15DF(`choosen`);
     nþ0E27F?.þF09E5(`choosen`);
     return Ѧ.Ⴝ
   }
   if (Ѧ.init) Ѧ.set(Ѧ.Ⴝ, Ѧ.init);
   return þ0E27F;
 }

 const ədevDisplay = (𝐮, 𝐝) => {
   const clr = `#${𝐝 . online ? `8CD521` : `D54C21`}`;

   const name1 = 𝐝.NAME || 𝐮;
   const name2 = name1 != 𝐮 ? 𝐮 : undefined;
   const þ0E27F = mkə(`div`, [
     [`𝑆`, `position: relative; min-height: 125px; display: flex; flex-direction: column; align-items: flex-start; padding-left: 10px; padding-right: 10px; padding-top: 6px; padding-bottom: 6px; font-size: 20px; font-weight: 100;`]
   ], [(mkə(`T`, [
     [`𝑆`, `font-weight: 400; position: absolute; top: -7px; right: 10px; font-size: 50px; color: ${clr};`]
   ], `⬤`)), (mkə(`T`, [
     [`𝑆`, `font-weight: 400; font-size: 30px;`]
   ], `${name1}`)), (name2 ? mkə(`T`, false, `UUID: ${name2}`) : []), (mkə(`T`, false, `${𝐝 . RGB_ORDER} - ${𝐝 . LEDC} pixels`)), (mkə(`T`, false, `Direction: ${[`For` , `Back`][ + 𝐝 . REVERSE]}wards`))]);
   return þ0E27F
 };
 const ədevTile = (Ѧ, [𝐮, 𝐝], rem) => {
   const set = (𝚜) => Bþ0E27F.ᴍþF01A9((x, ...𝔸) => x.ᴍ(þF0EFE)).þF01A9().ᴍ((x, ...𝔸) => x.disabled = !𝚜);
   const [𝐭, 𝐧, 𝔡] = [Ѧ.𝔱.𝐭, 𝐝.NAME ?? "", Ѧ.𝔱.dev(𝐮)];

   const þ0F044ƒ = () => əpromptGeneral(async (_0, 𝚗, _1, 𝚝) => {
       set(false);
       const v = [𝚗.𝚒.value, 𝚝.Ѧ.Ⴝ];
       const þF0B0A = {};
       if (v[0] != 𝐧) þF0B0A.NAME = v[0];
       if (v[1] != 𝐭) þF0B0A.TOKEN = v[1];
       if (!𝒪𝑏(þF0B0A)) return set(true);
       await 𝔡.config(þF0B0A);
       if (þF0B0A.TOKEN !== undefined) return rem();
       Ѧ.populate();
       set(true);
     },
     `Edit device info`,
     mkə(`T`, [
       [`𝑆`, `font-size: 1.6em;`]
     ], `Name`),
     textField({
       𝑃𝐻: `Name`,
       þ0F070: true,
       value: 𝐧
     }),
     mkə(`T`, [
       [`𝑆`, `font-size: 1.6em;`]
     ], `Token`),
     ədropdown(get_toks(), {
       Ⴝ: 𝐭
     }));
   const þ0F021ƒ = async () => {
     set(false);
     await 𝔡.config({
       REVERSE: !𝐝.REVERSE
     });
     Ѧ.populate();
     set(true);
   };
   const Bþ0E27F = [
     əbuttonBar(["", (...𝔸) => prompt_rgb_calibrate(𝔡, (x, ...𝔸) => (x, Ѧ.populate()))],
       ["", (...𝔸) => prompt_led_calibrate(𝔡, (x, ...𝔸) => (x, Ѧ.populate()))],
       ["󰐥", (...𝔸) => 𝔡.off()],
       ["", (...𝔸) => Bþ0E27F[1].þF09E5(`dropped`, Bþ0E27F[1].þF1021(`dropped`) == "✓" ? "✗" : "✓")]),
     əbuttonBar(["", þ0F044ƒ],
       ["", þ0F021ƒ])
   ]
   Bþ0E27F[1].þF09E5(`dropped`);
   return [ədevDisplay(𝐮, 𝐝), ...Bþ0E27F];
 };
 const ədevListing = (Ѧ = {}) => {
   Ѧ = {
     𝔱: undefined,
     ƒ: þF0EFE,
     ...Ѧ
   };
   const þ0E27F = əitemGridEditable(undefined,
     (x, rem) => ədevTile(Ѧ, x, rem),
     (x, ...𝔸) => print(`Removing:`, x),
     [], {
       show_plus: false,
       placeholder: (...𝔸) => mkə(`h2`, false, `No devices found for this token...`)
     })
   Ѧ = þ0E27F.Ѧ = {
     ...þ0E27F.Ѧ,
     ...Ѧ,
     þ0E27F
   };
   Ѧ.populate = async (𝔱, call = true) => {
     if (𝔱) Ѧ.𝔱 = 𝔱;
     let devs = await load_devices(Ѧ.𝔱);
     devs = devs[1];
     Ѧ.clear();
     𝒪(devs).forEach((x, ...𝔸) => Ѧ.add_item(x));
     if (call) Ѧ.ƒ(devs);
     return devs;
   };
   return þ0E27F;
 };

 const əscenePromptDev = (𝐮, 𝐝) => {

   const name1 = 𝐝.NAME || 𝐮;
   const name2 = name1 != 𝐮 ? 𝐮 : undefined;
   const þF0284 = `${name1}${name2 ? ` (${name2})` :  "" }`;
   const þ0E27F = mkə(`span`, [
     [`𝐶`, `devDraggable`]
   ], [`󰍜`, mkə(`div`, false, ``), mkə(`T`, [
     [`𝑆`, `text-align: right;`]
   ], þF0284)]);
   þ0E27F.𝐮 = 𝐮;
   return dragItem(þ0E27F);
 };
 const prompt_scene = (Ѧ, cb, 𝐬 = {
   name: "",
   𝐔: [],
   þF0832: {}
 }) => {
   const nameþ0E27F = textField({
     𝑃𝐻: `Scene Name`,
     value: 𝐬.name
   });

   const devsLþ0E27F = dragHold(mkə(`div`, [
     [`𝐶`, `devDraggableHolder`]
   ], ``));
   const devsRþ0E27F = dragHold(mkə(`div`, [
     [`𝐶`, `devDraggableHolder`]
   ], ``));

   const 𝐃 = get_devs(Ѧ.𝔱);
   𝐬.𝐔.forEach((𝐮) => devsRþ0E27F.append(əscenePromptDev(𝐮, 𝐃[𝐮])));
   𝒪k(𝐃).forEach((𝐮) => 𝐬.𝐔.includes(𝐮) || devsLþ0E27F.append(əscenePromptDev(𝐮, 𝐃[𝐮])));

   const get = () => {
     const 𝐔 = devsRþ0E27F.ᴍ((x, ...𝔸) => x.𝐮);
     const L = 𝐔.ᴍ((x, ...𝔸) => 𝐃[x].LEDC);
     const þF0832 = 𝒪𝑏(𝐬.þF0832) || þF0832Util.defM(L);
     return {
       ...𝐬,
       𝐔,
       þF0832,
       name: nameþ0E27F.𝚒.value
     }
   };
   const þ0E27F = popup({
       "": true,
       "󰅙": true
     },
     ətitledBox(
       `Scene details`,
       mkə(`div`, [
         [`𝐶`, `promptBox`],
         [`𝑆`, `max-width: ⅘; display: flex; flex-direction: column;`]
       ], [
         nameþ0E27F,
         mkə(`span`, [
           [`𝑆`, `display: flex; flex-direction: row; width: 90%; height: 325px; margin-bottom: 10px;`]
         ], [(mkə(`div`, [
           [`𝑆`, `display: flex; flex-direction: column; width: 50%; font-size: 26px;`]
         ], [`Unused`, devsLþ0E27F])), (mkə(`div`, [
           [`𝑆`, `display: flex; flex-direction: column; width: 50%; font-size: 26px;`]
         ], [`Used`, devsRþ0E27F]))]),
         textButton(`Submit`, (...𝔸) => {
           þ0E27F.close();
           cb(get())
         })
       ]), {
         outline: false
       }));
 };
 let əsceneListing = (Ѧ = {}) => {
   Ѧ = {
     ƒ: þF0EFE,
     ...Ѧ
   }
   const þ0E27F = əitemGridEditable(
     (gen) => prompt_scene(Ѧ,
       (𝐬) => {
         if (!add_scene(Ѧ.𝐭, 𝐬))
           return print(`Scene already exists ${x}‽`);
         gen(𝐬);
       }),
     (𝐬, rem) => {
       print(`Generating scene:`, 𝐬);
       const update = (𝚗) => {
         rem(𝐬);
         add_scene(Ѧ.𝐭, 𝚗, true);
         Ѧ.add_item(𝚗);
       };
       const clone = (...𝔸) => Ѧ.add_item({
         ...structuredClone(𝐬),
         name: `${𝐬 . name} - Copy`
       });
       const buttonþ0E27F = ətileButton(𝐬.name, () => Ѧ.ƒ({
         𝐬,
         𝔱: Ѧ.𝔱,
         þF0832: 𝐬.þF0832
       }));
       if (!𝐬.𝐔.length) buttonþ0E27F.disabled = true;
       return [buttonþ0E27F, əbuttonBar(["󰆴", rem],
         ["", (...𝔸) => prompt_editor(update, Ѧ.𝔱, 𝐬)],
         ["󰆏", clone],
         ["", (...𝔸) => prompt_scene(Ѧ, update, 𝐬)])];
     },
     (𝐬) => move_scene(Ѧ.𝐭, 𝐬.name));
   Ѧ = þ0E27F.Ѧ = {
     ...þ0E27F.Ѧ,
     ...Ѧ,
     þ0E27F
   };
   Ѧ.populate = (𝔱) => {
     if (𝔱) Ѧ.𝔱 = 𝔱;

     const 𝐭 = 𝔱?.𝐭;
     if (𝐭) Ѧ.𝐭 = 𝐭;

     þ0E27F.Ѧ.clear();
     𝒪(get_scenes(Ѧ.𝔱)).ꟿ((name, y) => Ѧ.add_item({
       name,
       ...y
     }));
   };
   return þ0E27F;
 }